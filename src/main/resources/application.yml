####
#Classpath and file-based solution
####

server:
   port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/velocodealer/config-provider/
          searchPaths: vcda-dealer-pricing-rules-provider.gitignore